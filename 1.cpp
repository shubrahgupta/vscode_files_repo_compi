#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define vi vector<int>
#define vs  vector<string>
#define vll vector<long long>
#define endl "\n"
#define pb push_back


#define f(i,s,e) for(long long int i=s;i<e;i++)
#define cf(i,s,e) for(long long int i=s;i<=e;i++)
#define rf(i,e,s) for(long long int i=e-1;i>=s;i--)
#define cap(t) transform(t.begin(), t.end(), t.begin(), ::toupper); //t is a string
#define low(t) transform(t.begin(), t.end(), t.begin(), ::tolower);
#define string_split(s,delimiter) size_t last = 0; size_t next = 0; while ((next = s.find(delimiter, last)) != string::npos) {   cout << s.substr(last, next-last) << endl;   last = next + 1; } cout << s.substr(last) << endl;
#define fill2D(arr,M,N,s) std::fill_n(&arr[0][0], M*N, s) // M,N = dimensions, s = value to be filled

template <class T>
void print_v(vector<T> &v) { for (auto x : v) cout << x << endl; }


int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);


	
	
 //    auto newEnd = std::remove(std::begin(e), std::end(e), ".");
	// *newEnd = '\0';
	// f(i,0,e.size()) {
	// 	f(j,0,e[i].size()) {
	// 		cout << e[i][j] << " ";
	// 	}
	// 	cout << endl;
		
	// }
	// int t;
	// cin >> t;
	
	// while(t--) {

		
	// }



	return 0;
} 

// O(n^2) + O(t*n^2)
